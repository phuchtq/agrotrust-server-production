basePath: /
definitions:
  entities.BankProfile:
    properties:
      bank_code:
        type: string
      bank_org:
        type: string
      created_at:
        type: string
      id:
        type: string
      owner:
        type: string
      owner_name:
        type: string
      payos_api_key:
        type: string
      payos_check_sum_key:
        type: string
      payos_client_id:
        type: string
      profile_id:
        type: string
      updated_at:
        type: string
    type: object
  entities.CenterRequest:
    properties:
      address:
        type: string
      approvers:
        items:
          type: string
        type: array
      closed_at:
        type: string
      created_at:
        type: string
      created_by:
        type: string
      id:
        type: string
      image_blob_id:
        type: string
      is_confirm_register:
        description: Default as false, when status aprroved, user clicks to update
          to true, call smart contract to register role
        type: boolean
      isAvailableToConfirm:
        type: boolean
      phone_number:
        type: string
      profile_id:
        type: string
      refuse_reasons:
        items:
          type: string
        type: array
      refusers:
        items:
          type: string
        type: array
      region:
        type: string
      status:
        description: e.g. "Pending", "Approved", "Refused"
        type: string
      updated_at:
        type: string
    type: object
  entities.ChildGuardianProfile:
    properties:
      full_name:
        type: string
      identity_card_blob_id:
        type: string
      phone_number:
        type: string
      relation:
        type: string
    type: object
  entities.PendingChildSpecialNeedProposal:
    properties:
      actor_address:
        type: string
      actor_profile_id:
        type: string
      ai_evaluation:
        type: string
      child_id:
        type: string
      created_at:
        type: string
      description:
        type: string
      id:
        type: string
      proof_blob_id:
        type: string
      region:
        type: string
      review_status:
        type: string
      reviewed_by:
        type: string
      target:
        type: integer
      updated_at:
        type: string
    type: object
  entities.PendingWithdrawProposal:
    properties:
      aievaluation:
        type: string
      createdAt:
        type: string
      creator:
        type: string
      description:
        type: string
      id:
        type: string
      poolID:
        type: string
      poolName:
        type: string
      profileID:
        type: string
      proofBlobID:
        type: string
      purpose:
        type: string
      reviewedBy:
        type: string
      status:
        type: string
      target:
        type: string
      updatedAt:
        type: string
      withdrawAmount:
        type: integer
    type: object
  entities.RegistrationRequest:
    properties:
      approvers:
        items:
          type: string
        type: array
      avatar_blob_id:
        type: string
      closed_at:
        type: string
      created_at:
        type: string
      created_by:
        type: string
      date_of_birth:
        type: string
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      id:
        type: string
      identity_card_blob_id:
        type: string
      identity_code:
        type: string
      is_confirm_register:
        description: Default as false, when status aprroved, user clicks to update
          to true, call smart contract to register role
        type: boolean
      isAvailableToConfirm:
        type: boolean
      last_name:
        type: string
      phone_number:
        type: string
      profile_id:
        type: string
      refuse_reasons:
        items:
          type: string
        type: array
      refusers:
        items:
          type: string
        type: array
      region:
        type: string
      register_role:
        type: string
      status:
        description: e.g. "Pending", "Approved", "Refused"
        type: string
      updated_at:
        type: string
    type: object
  entities.SupportedRegionSuggestion:
    properties:
      content:
        type: string
      createdAt:
        type: string
      createdBy:
        type: string
      id:
        type: string
      profileID:
        type: string
      region:
        type: string
      reviewedBy:
        type: string
      status:
        type: string
      updatedAt:
        type: string
    type: object
  entities.UploadChildRequest:
    properties:
      ai_evaluation:
        type: string
      approvers:
        items:
          type: string
        type: array
      avatar_blob_id:
        type: string
      closed_at:
        type: string
      created_at:
        type: string
      created_by:
        type: string
      date_of_birth:
        type: string
      first_guardian_profile:
        $ref: '#/definitions/entities.ChildGuardianProfile'
      first_name:
        type: string
      gender:
        type: string
      home_address:
        type: string
      home_blob_id:
        type: string
      id:
        type: string
      identity_code:
        type: string
      is_confirm_upload:
        description: Default as false, when status aprroved, user clicks to update
          to true, call smart contract to register role
        type: boolean
      last_name:
        type: string
      profile_id:
        type: string
      refuse_reasons:
        items:
          type: string
        type: array
      refusers:
        items:
          type: string
        type: array
      region:
        type: string
      review_status:
        description: e.g. "Pending", "Approved", "Refused"
        type: string
      reviewed_by:
        type: string
      second_guardian_profile:
        $ref: '#/definitions/entities.ChildGuardianProfile'
      status:
        description: e.g. "Pending", "Approved", "Refused"
        type: string
      updated_at:
        type: string
    required:
    - avatar_blob_id
    - date_of_birth
    - first_name
    - gender
    - last_name
    - region
    type: object
  request.AddChildStringMetadaRequest:
    properties:
      key:
        type: string
      value:
        type: string
    required:
    - key
    - value
    type: object
  request.ChildGuardianProfile:
    properties:
      guardian_full_name:
        type: string
      guardian_phone_number:
        type: string
      guardian_relation:
        type: string
      identity_card_blob_id:
        type: string
    required:
    - guardian_full_name
    - guardian_phone_number
    - guardian_relation
    - identity_card_blob_id
    type: object
  request.ConfirmProvideMealForChildRequest:
    properties:
      image_blob_id:
        type: string
    required:
    - image_blob_id
    type: object
  request.ConfirmReceiveGiftRequest:
    properties:
      delivered_image_blob_id:
        type: string
    required:
    - delivered_image_blob_id
    type: object
  request.CreateBankProfileRequest:
    properties:
      bank_code:
        type: string
      bank_org:
        type: string
      owner_name:
        type: string
      payos_api_key:
        type: string
      payos_check_sum_key:
        type: string
      payos_client_id:
        type: string
    required:
    - bank_code
    - bank_org
    - owner_name
    type: object
  request.CreateCenterRequest:
    properties:
      address:
        type: string
      image_blob_id:
        type: string
      phone_number:
        type: string
      region:
        type: string
    required:
    - address
    - image_blob_id
    - phone_number
    - region
    type: object
  request.CreateGiftRequest:
    properties:
      carrier:
        type: string
      category:
        type: string
      description:
        type: string
      gift_image_blob_id:
        type: string
      gift_value:
        minimum: 2000
        type: integer
      message:
        type: string
      recipient:
        type: string
      tracking_code:
        type: string
    required:
    - carrier
    - category
    - description
    - gift_image_blob_id
    - gift_value
    - message
    - recipient
    - tracking_code
    type: object
  request.CreateNormalNeedWithdrawProposalRequest:
    properties:
      need_id:
        type: string
      proof_blob_id:
        type: string
    required:
    - need_id
    type: object
  request.CreatePendingWithdrawProposalRequest:
    properties:
      description:
        type: string
      pool_id:
        type: string
      proof_blob_id:
        type: string
      withdraw_amount:
        maximum: 20000000
        minimum: 10000
        type: integer
    required:
    - description
    - pool_id
    - withdraw_amount
    type: object
  request.CreateRegistrationRequest:
    properties:
      avatar_blob_id:
        type: string
      identity_card_blob_id:
        type: string
      region:
        type: string
      register_role:
        type: string
    required:
    - avatar_blob_id
    - identity_card_blob_id
    - region
    - register_role
    type: object
  request.CreateSpecialNeedProposalRequest:
    properties:
      child_id:
        type: string
      description:
        type: string
      proof_blob_id:
        type: string
      target:
        minimum: 100000
        type: integer
    required:
    - child_id
    - description
    - target
    type: object
  request.CreateSpecialNeedWithdrawProposalRequest:
    properties:
      amount:
        minimum: 2000
        type: integer
      campaign_id:
        type: string
      description:
        type: string
      proof_blob_id:
        type: string
    required:
    - amount
    - campaign_id
    - description
    type: object
  request.CreateSupportedRegionSuggestionsRequest:
    properties:
      content:
        type: string
      region:
        type: string
    required:
    - content
    - region
    type: object
  request.CreateWithdrawProposalRequest:
    properties:
      description:
        type: string
      pool_id:
        type: string
      proof_blob_id:
        type: string
      withdraw_amount:
        maximum: 20000000
        minimum: 10000
        type: integer
    required:
    - description
    - pool_id
    - withdraw_amount
    type: object
  request.DonateRequest:
    properties:
      amount:
        minimum: 2000
        type: integer
      message:
        type: string
      pool_id:
        type: string
    required:
    - amount
    - pool_id
    type: object
  request.ExecuteTransactionRequest:
    properties:
      center_req:
        type: string
      proposal_id:
        type: string
      registration_req:
        type: string
      signature:
        type: string
      tx_bytes:
        type: string
      upload_child_req:
        type: string
    required:
    - signature
    - tx_bytes
    type: object
  request.LoginRequestV2:
    properties:
      address:
        type: string
      sub:
        type: string
    required:
    - address
    - sub
    type: object
  request.SupportMealNeadRequest:
    properties:
      months:
        maximum: 12
        minimum: 1
        type: integer
    required:
    - months
    type: object
  request.SupportSpecialNeedRequest:
    properties:
      amount:
        minimum: 2000
        type: integer
      description:
        type: string
    required:
    - amount
    - description
    type: object
  request.UpdateBankProfileRequest:
    properties:
      bank_code:
        type: string
      bank_org:
        type: string
      owner_name:
        type: string
      payos_api_key:
        type: string
      payos_check_sum_key:
        type: string
      payos_client_id:
        type: string
    type: object
  request.UpdatePublisherInfoRequest:
    properties:
      avatar_blob_id:
        type: string
      date_of_birth:
        type: string
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      identity_card_blob_id:
        type: string
      identity_code:
        type: string
      last_name:
        type: string
      phone_number:
        type: string
    required:
    - avatar_blob_id
    - date_of_birth
    - email
    - first_name
    - gender
    - identity_card_blob_id
    - identity_code
    - last_name
    - phone_number
    type: object
  request.UploadChildRequest:
    properties:
      avatar_blob_id:
        type: string
      date_of_birth:
        type: string
      first_guardian:
        $ref: '#/definitions/request.ChildGuardianProfile'
      first_name:
        type: string
      gender:
        type: string
      home_address:
        type: string
      home_blob_id:
        type: string
      identity_code:
        type: string
      last_name:
        type: string
      region:
        type: string
      second_guardian:
        $ref: '#/definitions/request.ChildGuardianProfile'
    required:
    - avatar_blob_id
    - date_of_birth
    - first_guardian
    - first_name
    - gender
    - home_address
    - home_blob_id
    - identity_code
    - last_name
    - region
    type: object
  request.UploadProfileRequest:
    properties:
      date_of_birth:
        type: string
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      identity_code:
        type: string
      last_name:
        type: string
      phone_number:
        type: string
      region:
        type: string
    required:
    - date_of_birth
    - email
    - first_name
    - gender
    - identity_code
    - last_name
    - phone_number
    - region
    type: object
  request.VoteRequest:
    properties:
      is_vote_yes:
        type: boolean
      refuse_reason:
        type: string
    required:
    - is_vote_yes
    type: object
  response.BankProfileResponse:
    properties:
      bank_code:
        type: string
      bank_org:
        type: string
      id:
        type: string
      owner:
        type: string
      owner_name:
        type: string
    type: object
  response.BuildTransactionResponse:
    properties:
      center_req:
        type: string
      proposal_id:
        type: string
      registration_req:
        type: string
      tx_bytes:
        type: string
      upload_child_req:
        type: string
    type: object
  response.ChildResponse:
    properties:
      avatar_blob_id:
        type: string
      books_needs:
        items:
          type: string
        type: array
      date_of_birth:
        type: string
      dynamic_fields:
        items:
          type: string
        type: array
      dynamic_values:
        additionalProperties: true
        type: object
      first_name:
        type: string
      gender:
        type: string
      gifts:
        items:
          type: string
        type: array
      id:
        type: string
      identity_code:
        type: string
      image_blob_ids:
        items:
          type: string
        type: array
      last_name:
        type: string
      meal_need:
        type: string
      region:
        type: string
      special_need_campaigns:
        items:
          type: string
        type: array
      special_need_proposals:
        items:
          type: string
        type: array
      updated_at:
        type: string
      upload_image_periods:
        items:
          type: string
        type: array
      uploaded_at:
        type: string
      uploaded_by:
        type: string
    type: object
  response.DonorResponse:
    properties:
      contributions:
        items:
          $ref: '#/definitions/response.TransactionResponse'
        type: array
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      id:
        type: string
      last_name:
        type: string
      name:
        type: string
      phone_number:
        type: string
      total_donation:
        type: integer
      url:
        type: string
    type: object
  response.GiftResponse:
    properties:
      cancel_reason:
        type: string
      carrier:
        type: string
      category:
        type: string
      confirm_recieved_by:
        type: string
      delivered_at:
        type: string
      delivered_image_blob_id:
        type: string
      description:
        type: string
      gift_image_blob_id:
        type: string
      id:
        type: string
      is_for_child:
        type: boolean
      message:
        type: string
      recipient:
        type: string
      sender:
        type: string
      status:
        type: string
      tracking_code:
        type: string
      updated_at:
        type: string
      uploaded_at:
        type: string
    type: object
  response.MessageAPIResponse:
    properties:
      message:
        type: string
    type: object
  response.PaginationDataResponse:
    properties:
      amount:
        type: integer
      data: {}
      page:
        type: integer
      total_pages:
        type: integer
    type: object
  response.PersonalProfileResponse:
    properties:
      date_of_birth:
        type: string
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      id:
        type: string
      identity_code:
        type: string
      last_name:
        type: string
      phone_number:
        type: string
    type: object
  response.RegionsResponse:
    properties:
      regions:
        items:
          type: string
        type: array
    type: object
  response.StaffNftResponse:
    properties:
      avatar_blob_id:
        type: string
      date_of_birth:
        type: string
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      id:
        type: string
      identity_card_blob_id:
        type: string
      identity_code:
        type: string
      last_name:
        type: string
      name:
        type: string
      phone_number:
        type: string
      region:
        type: string
      role:
        type: string
      uploaded_at:
        type: string
      url:
        type: string
    type: object
  response.StaffResponse:
    properties:
      avatar_blob_id:
        type: string
      date_of_birth:
        type: string
      email:
        type: string
      first_name:
        type: string
      gender:
        type: string
      id:
        type: string
      identity_code:
        type: string
      last_name:
        type: string
      nfts:
        items:
          $ref: '#/definitions/response.StaffNftResponse'
        type: array
      phone_number:
        type: string
      region:
        type: string
      string:
        type: string
      uploaded_at:
        type: string
      user:
        type: string
    type: object
  response.TransactionResponse:
    properties:
      action_type:
        description: e.g., "Deposit", "Withdraw"
        type: string
      actor_address:
        type: string
      amount:
        type: integer
      coin_type:
        description: e.g., "Sui", "USDC"
        type: string
      created_at:
        type: string
      id:
        type: string
      message:
        type: string
      pool_name:
        type: string
    type: object
  response.UrlAPIResponse:
    properties:
      url:
        type: string
    type: object
  response.WithdrawProposalResponse:
    properties:
      approve_weight:
        type: integer
      approved_periods:
        items:
          type: string
        type: array
      approvers:
        items:
          type: string
        type: array
      closed_at:
        type: string
      created_at:
        type: string
      creator:
        type: string
      description:
        type: string
      id:
        type: string
      is_executed:
        type: boolean
      is_from_local_pool:
        type: boolean
      pool_name:
        type: string
      refuse_reasons:
        items:
          type: string
        type: array
      refuse_weight:
        type: integer
      refused_periods:
        items:
          type: string
        type: array
      refusers:
        items:
          type: string
        type: array
      updated_at:
        type: string
      withdraw_amount:
        type: integer
    type: object
host: locahost:8080
info:
  contact: {}
  description: API for AgroTrust Server
  title: AgroTrust Server API
  version: "1.0"
paths:
  /admins:
    get:
      consumes:
      - application/json
      description: Retrieves a list of admins based on filter criteria
      parameters:
      - in: query
        name: gender
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: sort_criteria
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: year_of_birth
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List admins
      tags:
      - admins
    post:
      consumes:
      - application/json
      description: Updates the details of publisher (admin) based on the provided
        request body with just only 1 call permitted.
      parameters:
      - description: Publisher Update Information
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.UpdatePublisherInfoRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Update publisher information
      tags:
      - admins
  /auth/login:
    post:
      consumes:
      - application/json
      description: Authenticates a user and returns access tokens/session data
      parameters:
      - description: Login credentials (e.g., address and sub)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.LoginRequestV2'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: User login
      tags:
      - auth
  /auth/logout:
    post:
      consumes:
      - application/json
      description: Invalidates the session for the specified user address
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Logout user
      tags:
      - auth
  /auth/nonce/{address}:
    get:
      consumes:
      - application/json
      description: Retrieves a unique nonce for the user to sign
      parameters:
      - description: User wallet address
        in: path
        name: address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get login nonce
      tags:
      - auth
  /auth/salt/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves a unique salt for the user to login
      parameters:
      - description: User sub
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get wallet salt
      tags:
      - auth
  /banks:
    post:
      consumes:
      - application/json
      description: Submit bank details to create a new profile in the system
      parameters:
      - description: Bank Profile Creation Data
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateBankProfileRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.BankProfile'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new bank profile
      tags:
      - banks
  /banks/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve detailed bank profile information by its unique id
      parameters:
      - description: Bank Profile ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BankProfileResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get bank profile by specific id
      tags:
      - banks
    put:
      consumes:
      - application/json
      description: Modify the details of an existing bank profile by its ID
      parameters:
      - description: Bank Profile ID
        in: path
        name: id
        required: true
        type: string
      - description: Updated Bank Profile Data
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.UpdateBankProfileRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/entities.BankProfile'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Update an existing bank profile
      tags:
      - banks
  /banks/user/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve detailed bank profile information using the bank's owner
        wallet
      parameters:
      - description: Wallet address
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BankProfileResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get bank profile owned by a wallet
      tags:
      - banks
  /centers:
    get:
      consumes:
      - application/json
      description: Retrieve center requests with filtering based on query parameters
      parameters:
      - in: query
        name: is_available_to_confirm
        type: boolean
      - in: query
        name: is_closed
        type: boolean
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a list of center requests
      tags:
      - center
    post:
      consumes:
      - application/json
      description: Submit a new center request
      parameters:
      - description: Center Request Body
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateCenterRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.CenterRequest'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new center request
      tags:
      - center
  /centers/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve the full details of a single Center request by its unique
        ID
      parameters:
      - description: Center Request ID (UUID)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get center request details
      tags:
      - center
  /centers/{id}/confirm:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for uploading center information
        on-chain
      parameters:
      - description: Center Request ID (UUID)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm and upload center information to Sui Blockhain
      tags:
      - center
  /centers/{id}/vote:
    post:
      consumes:
      - application/json
      description: Submit an approval or refusal vote for a specific center request.
      parameters:
      - description: Center Request ID
        in: path
        name: id
        required: true
        type: string
      - description: Vote Details
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.VoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Vote on a center request
      tags:
      - center
  /centers/user/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve specific center requests associated with a unique wallet
        address
      parameters:
      - description: User Wallet Address
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get Center requests by a wallet
      tags:
      - center
  /child-upload-reqs:
    get:
      consumes:
      - application/json
      description: Retrieve a list of child upload requests based on query parameters.
      parameters:
      - in: query
        name: gender
        type: string
      - in: query
        name: is_closed
        type: boolean
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get upload-child requests
      tags:
      - Child Upload Request
    post:
      consumes:
      - application/json
      description: Submit a JSON payload to create a new child upload request in the
        system.
      parameters:
      - description: Upload-child request payload
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.UploadChildRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.UploadChildRequest'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new upload-child request
      tags:
      - Child Upload Request
  /child-upload-reqs/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve the full details of a single upload-child request by its
        unique ID
      parameters:
      - description: Upload-child Request ID (UUID)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get upload-child request details
      tags:
      - Child Upload Request
  /child-upload-reqs/{id}/confirm:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for registering new child information
        on-chain
      parameters:
      - description: UploadChild Request ID (UUID)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm and store a child information to Sui Blockhain
      tags:
      - Child Upload Request
  /child-upload-reqs/{id}/review:
    post:
      consumes:
      - application/json
      description: Submit an approval or refusal vote for reviewing a specific child
        upload request using its ID.
      parameters:
      - description: Upload Child Request ID
        in: path
        name: id
        required: true
        type: string
      - description: Voting details (e.g., vote type, comments)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.VoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Review an upload-child request
      tags:
      - Child Upload Request
  /child-upload-reqs/{id}/vote:
    post:
      consumes:
      - application/json
      description: Submit an approval or refusal vote for a specific child upload
        request using its ID.
      parameters:
      - description: Upload Child Request ID
        in: path
        name: id
        required: true
        type: string
      - description: Voting details (e.g., vote type, comments)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.VoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Vote on an upload-child request
      tags:
      - Child Upload Request
  /child-upload-reqs/user/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve child upload requests associated with a specific wallet
        and page number.
      parameters:
      - description: Wallet address
        in: path
        name: id
        required: true
        type: string
      - default: 1
        description: Page number for pagination
        in: query
        name: page
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get upload-child requests by a wallet
      tags:
      - Child Upload Request
  /children:
    get:
      consumes:
      - application/json
      description: Retrieves a list of children based on filter criteria
      parameters:
      - in: query
        name: gender
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: year_of_birth
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List children
      tags:
      - children
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for uploading new child information
        (e.g., "height", "weight") on-chain
      parameters:
      - description: 'Child Information (e.g. '
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.UploadChildRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Upload a new child to Sui Blockchain
      tags:
      - children
  /children/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves child information by its unique ID
      parameters:
      - description: Child ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.ChildResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get child details
      tags:
      - children
  /children/{id}/provide-meal/confirm:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for confirming provide meal for
        a child with specific ID on-chain
      parameters:
      - description: Child ID
        in: path
        name: id
        required: true
        type: string
      - description: Confirm Provide Meal For Child Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.ConfirmProvideMealForChildRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm provide meal for child
      tags:
      - children
  /children/books-need/{id}/support:
    post:
      consumes:
      - application/json
      description: Support books need for a child
      parameters:
      - description: Books Need ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.UrlAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Support books need for a child
      tags:
      - children
  /children/books-need/withdraw-proposal:
    post:
      consumes:
      - application/json
      description: Create books need withdraw proposal from a child and wait for admin
        to review the proposal
      parameters:
      - description: Create Books Need Withdraw Proposal Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateNormalNeedWithdrawProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.PendingWithdrawProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create books need withdraw proposal
      tags:
      - children
  /children/health-insurance-need/{id}/support:
    post:
      consumes:
      - application/json
      description: Support health insurance need for a child
      parameters:
      - description: Health Insurance Need ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.UrlAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Support health insurance need for a child
      tags:
      - children
  /children/health-insurance-need/withdraw-proposal:
    post:
      consumes:
      - application/json
      description: Create health insurance need withdraw proposal from a child and
        wait for admin to review the proposal
      parameters:
      - description: Create Health Insurance Need Withdraw Proposal Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateNormalNeedWithdrawProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.PendingWithdrawProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create health insurance need withdraw proposal
      tags:
      - children
  /children/meal-need/{id}/support:
    post:
      consumes:
      - application/json
      description: Support meal need for a child
      parameters:
      - description: Meal Need ID
        in: path
        name: id
        required: true
        type: string
      - description: Support Child Meal Need Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.SupportMealNeadRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.UrlAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Support meal need for a child
      tags:
      - children
  /children/meal-need/withdraw-proposal:
    post:
      consumes:
      - application/json
      description: Create meal need withdraw proposal from a child and wait for admin
        to review the proposal
      parameters:
      - description: Create Meal Need Withdraw Proposal Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateNormalNeedWithdrawProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.PendingWithdrawProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create meal need withdraw proposal
      tags:
      - children
  /children/metadata/number/{id}:
    put:
      consumes:
      - application/json
      description: Prepares and builds a transaction for adding new field information
        to a child information (e.g., height, weight) on-chain
      parameters:
      - description: Add child number metadata details (e.g., key, value)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.AddChildStringMetadaRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Add a new number metadata to a child object
      tags:
      - children
  /children/metadata/string/{id}:
    put:
      consumes:
      - application/json
      description: Prepares and builds a transaction for adding new field information
        to a child information (e.g., height, weight) on-chain
      parameters:
      - description: Add child string metadata details (e.g., key, value)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.AddChildStringMetadaRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Add a new string metadata to a child object
      tags:
      - children
  /children/special-need/{id}/support:
    post:
      consumes:
      - application/json
      description: Support special need campaign of a child
      parameters:
      - description: Campaign ID
        in: path
        name: id
        required: true
        type: string
      - description: Support Child Special Need Campaign Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.SupportSpecialNeedRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.UrlAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Support special need campaign of a child
      tags:
      - children
  /children/special-need/proposal:
    post:
      consumes:
      - application/json
      description: Create special need proposal for a child and wait for admin to
        review the proposal
      parameters:
      - description: Create Special Need Campaign Withdraw Proposal Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateSpecialNeedProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.PendingChildSpecialNeedProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create special need proposal
      tags:
      - children
  /children/special-need/proposal/{id}/confirm:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for confirming a special need
        proposal for a child if accepted to create a new campaign on-chain
      parameters:
      - description: Special Need Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm a special need proposal
      tags:
      - children
  /children/special-need/proposal/{id}/vote:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for voting a special need proposal
        of a child on-chain
      parameters:
      - description: Special Need Proposal ID
        in: path
        name: id
        required: true
        type: string
      - description: Voting Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.VoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Vote a special need proposal
      tags:
      - children
  /children/special-need/withdraw-proposal:
    post:
      consumes:
      - application/json
      description: Create special need withdraw proposal from a child and wait for
        admin to review the proposal
      parameters:
      - description: Create Special Need Campaign Withdraw Proposal Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateSpecialNeedWithdrawProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.PendingWithdrawProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create special need campaign withdraw proposal
      tags:
      - children
  /donors:
    get:
      consumes:
      - application/json
      description: Retrieves a list of donors based on filter criteria
      parameters:
      - in: query
        name: gender
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List donors
      tags:
      - donor
  /donors/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves donor information by unique ID
      parameters:
      - description: Donor ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.DonorResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get donor details
      tags:
      - donor
  /gifts:
    post:
      consumes:
      - application/json
      description: Prepare and create a new gift for a child on-chain
      parameters:
      - description: Create gift details
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateGiftRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new gift for a child
      tags:
      - gift
  /gifts/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve details of a specific gift by its ID
      parameters:
      - description: Gift ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.GiftResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a gift information
      tags:
      - gift
  /gifts/{id}/confirm:
    post:
      consumes:
      - application/json
      description: Finalize and confirm a specific withdraw proposal by ID
      parameters:
      - description: Gift ID
        in: path
        name: id
        required: true
        type: string
      - description: Confirm receive gift details
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.ConfirmReceiveGiftRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm a withdraw proposal
      tags:
      - gift
  /gifts/child/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve a list of gifts sent to a child with optional query filters
      parameters:
      - description: Child ID
        in: path
        name: id
        required: true
        type: string
      - in: query
        name: category
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List gifts of a child
      tags:
      - gift
  /notis/user/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves a list of notifications from a user based on filter criteria
      parameters:
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - description: Wallet Address
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: List notifications of a user
      tags:
      - notification
  /payments/auth-callback/{id}:
    get:
      consumes:
      - application/json
      description: Handles the transaction callback/webhook from the payment provider
        to update transaction status by ID and build on-chain transaction.
      parameters:
      - description: Payment ID
        in: path
        name: id
        required: true
        type: string
      - description: Captured Image Blob ID
        in: query
        name: imageBlobId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Process payment callback with authorization
      tags:
      - payment
  /payments/callback/{id}:
    get:
      consumes:
      - application/json
      description: Handles the transaction callback/webhook from the payment provider
        to update transaction status by ID and build on-chain transaction.
      parameters:
      - description: Payment ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Process payment callback
      tags:
      - payment
  /payments/donate:
    post:
      consumes:
      - application/json
      description: Initiates a donation process, binds the request JSON, and returns
        payment or redirect information.
      parameters:
      - description: Donation Details
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.DonateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.UrlAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Process a donation
      tags:
      - payment
  /pending-special-needs:
    get:
      consumes:
      - application/json
      description: Retrieves a list of child pending special need proposals based
        on filter criteria
      parameters:
      - in: query
        name: creator
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: max_amount
        type: integer
      - in: query
        name: min_amount
        type: integer
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: reviewer
        type: string
      - in: query
        name: sort_criteria
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: List Child Pending Special Need Proposals
      tags:
      - pending-special-needs
  /pending-special-needs/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves child pending special need proposal information by its
        unique ID
      parameters:
      - description: Pending Child Special Need Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/entities.PendingChildSpecialNeedProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Get Child Pending Special Need Proposal Detail
      tags:
      - pending-special-needs
  /pending-special-needs/{id}/approve:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for approve and upload child
        pending special need proposal on-chain
      parameters:
      - description: Pending Child Special Need Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Approve a Child Pending Special Need Proposal and upload to Sui Blockchain
      tags:
      - pending-special-needs
  /pending-special-needs/{id}/refuse:
    post:
      consumes:
      - application/json
      description: Refuse a Child Pending Special Need Proposal
      parameters:
      - description: Pending Child Special Need Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Refuse a Child Pending Special Need Proposal
      tags:
      - pending-special-needs
  /pending-withdraw-proposals:
    get:
      consumes:
      - application/json
      description: Retrieve a list of pending withdraw proposals with optional query
        filters
      parameters:
      - in: query
        name: creator
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: max_amount
        type: integer
      - in: query
        name: min_amount
        type: integer
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: reviewer
        type: string
      - in: query
        name: sort_criteria
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: List pending withdraw proposals
      tags:
      - Pending Withdrawal
    post:
      consumes:
      - application/json
      description: Create a new pending withdraw proposal
      parameters:
      - description: 'Pending Withdraw Proposal details (e.g., '
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreatePendingWithdrawProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new pending withdraw proposal
      tags:
      - Pending Withdrawal
  /pending-withdraw-proposals/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve details of a specific pending withdraw proposal by its
        ID
      parameters:
      - description: Pending Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/entities.PendingWithdrawProposal'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Get a pending withdraw proposal
      tags:
      - Pending Withdrawal
  /pending-withdraw-proposals/{id}/approve:
    post:
      consumes:
      - application/json
      description: Approve a pending withdraw proposal to prepare and build a transaction
        to publish this proposal on chain
      parameters:
      - description: Oending Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Approve a pending withdraw proposal
      tags:
      - Pending Withdrawal
  /pending-withdraw-proposals/{id}/refuse:
    post:
      consumes:
      - application/json
      description: Refuse a pending withdraw proposal
      parameters:
      - description: Oending Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Refuse a pending withdraw proposal
  /profiles/{id}:
    post:
      consumes:
      - application/json
      description: Upload an account personal information based on profile ID
      parameters:
      - description: Profile ID
        in: path
        name: id
        required: true
        type: string
      - description: Profile Information
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.UploadProfileRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PersonalProfileResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Upload an account personal information
      tags:
      - profile
  /regions:
    get:
      consumes:
      - application/json
      description: Retrieves a list of all available regions
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.RegionsResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get list of regions
      tags:
      - regions
  /regions/admin/supported-suggestions:
    get:
      consumes:
      - application/json
      description: Retrieves a list of supported region proposals based on filter
        criteria as actor is admin
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Admin gets a list of supported region proposals
      tags:
      - regions
  /regions/supported-suggestions:
    get:
      consumes:
      - application/json
      description: Retrieves a list of supported region proposals based on filter
        criteria
      parameters:
      - in: query
        name: created_by
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: sort_order
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get list of supported region proposals
      tags:
      - regions
    post:
      consumes:
      - application/json
      description: Submit a new supported region proposal.
      parameters:
      - description: Supported Region Request Body
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateSupportedRegionSuggestionsRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.SupportedRegionSuggestion'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new supported region proposal
      tags:
      - regions
  /regions/supported-suggestions/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves a supported region proposal detailed information
      parameters:
      - description: Supported Region Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/entities.SupportedRegionSuggestion'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a supported region proposal detail
      tags:
      - regions
  /regions/supported-suggestions/{id}/review:
    post:
      consumes:
      - application/json
      description: Review a supported region proposal.
      parameters:
      - description: Review Region Suggestion Detail
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.VoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Review supported region proposal
      tags:
      - regions
  /regions/user/{id}/supported-suggestions:
    get:
      consumes:
      - application/json
      description: Retrieves a list of supported region proposals of a user based
        on filter criteria
      parameters:
      - description: User Wallet Address
        in: path
        name: id
        required: true
        type: string
      - in: query
        name: created_by
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: sort_order
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Get list of supported region proposals created by a user
      tags:
      - regions
  /registrations:
    get:
      consumes:
      - application/json
      description: Retrieve registration requests with filtering based on query parameters
      parameters:
      - in: query
        name: gender
        type: string
      - in: query
        name: is_available_to_confirm
        type: boolean
      - in: query
        name: is_closed
        type: boolean
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: register_role
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a list of registration requests
      tags:
      - registration
    post:
      consumes:
      - application/json
      description: Submit a new registration request. The phone number should be in
        a valid format (e.g., 090...) and email must be valid.
      parameters:
      - description: Registration Request Body
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateRegistrationRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/entities.RegistrationRequest'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new registration request
      tags:
      - registration
  /registrations/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve the full details of a single registration request by its
        unique ID
      parameters:
      - description: Registration Request ID (UUID)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get registration request details
      tags:
      - registration
  /registrations/{id}/confirm:
    post:
      consumes:
      - application/json
      description: Prepares and builds a transaction for registering new staff information
        on-chain
      parameters:
      - description: Registration Request ID (UUID)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm and register a staff role uploaded to Sui Blockhain
      tags:
      - registration
  /registrations/{id}/vote:
    post:
      consumes:
      - application/json
      description: Submit an approval or refusal vote for a specific registration
        request.
      parameters:
      - description: Registration Request ID
        in: path
        name: id
        required: true
        type: string
      - description: Vote Details
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.VoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Vote on a registration request
      tags:
      - registration
  /registrations/user/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve specific registration requests associated with a unique
        wallet address
      parameters:
      - default: 0x...
        description: User Wallet Address
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get registration requests by a wallet
      tags:
      - registration
  /staffs:
    get:
      consumes:
      - application/json
      description: Retrieves a list of staffs based on filter criteria
      parameters:
      - in: query
        name: gender
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: role
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: year_of_birth
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List staffs
      tags:
      - staffs
  /staffs/{id}:
    get:
      consumes:
      - application/json
      description: Retrieves staff information by its unique ID
      parameters:
      - description: Staff ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.StaffResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get staff details
      tags:
      - staffs
  /task-proofs:
    get:
      consumes:
      - application/json
      description: Retrieve a list of task proofs with optional query filters
      parameters:
      - in: query
        name: actor_address
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: reviewed_by
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List task proofs
      tags:
      - task-proofs
  /task-proofs/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve details of a task proof by its ID
      parameters:
      - description: Task Proof ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a task proof
      tags:
      - task-proofs
  /task-proofs/{id}/approve:
    post:
      consumes:
      - application/json
      description: Prepare and build a transaction for approve and upload a task proof
        on chain
      parameters:
      - description: Task Proof ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Approve a task proof
      tags:
      - task-proofs
  /task-proofs/{id}/refuse:
    post:
      consumes:
      - application/json
      description: Refuse a task proof
      parameters:
      - description: Task Proof ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Sucsess
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Refuse a task proof
      tags:
      - task-proofs
  /task-proofs/task/{id}/submit:
    post:
      consumes:
      - application/json
      description: Submit task proof for a task
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      - in: query
        name: image_blob_id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Sucsess
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Submit task proof for a task
      tags:
      - task-proofs
  /tasks:
    get:
      consumes:
      - application/json
      description: Retrieve a list of tasks with optional query filters
      parameters:
      - in: query
        name: assgined_staff
        type: string
      - in: query
        name: keyword
        type: string
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: region
        type: string
      - in: query
        name: reviewed_by
        type: string
      - in: query
        name: sort_order
        type: string
      - in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List tasks
      tags:
      - tasks
    post:
      consumes:
      - application/json
      description: Create a task
      parameters:
      - in: query
        name: description
        required: true
        type: string
      - in: query
        name: end_period
        required: true
        type: string
      - in: query
        name: region
        required: true
        type: string
      - in: query
        name: start_period
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a task
      tags:
      - tasks
  /tasks/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve details of a task by its ID
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a task
      tags:
      - tasks
  /tasks/{id}/claim:
    post:
      consumes:
      - application/json
      description: Claim a task
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Claim a task
      tags:
      - tasks
  /tasks/{id}/review:
    post:
      consumes:
      - application/json
      description: Review a task
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      - in: query
        name: is_vote_yes
        required: true
        type: boolean
      - in: query
        name: refuse_reason
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Review a task
      tags:
      - tasks
  /tx/execute:
    post:
      consumes:
      - application/json
      description: Processes a transaction request and executes it on the Sui Network
        blockchain
      parameters:
      - description: Transaction execution details
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.ExecuteTransactionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Execute an on-chain transaction
      tags:
      - transactions
  /withdraw-proposals:
    get:
      consumes:
      - application/json
      description: Retrieve a list of withdraw proposals with optional query filters
      parameters:
      - in: query
        name: creator
        type: string
      - in: query
        name: is_closed
        type: boolean
      - in: query
        name: is_executed
        type: boolean
      - description: Filter for pool ID, pool name, desc
        in: query
        name: keyword
        type: string
      - in: query
        name: max_amount
        type: integer
      - in: query
        name: min_amount
        type: integer
      - in: query
        name: page
        type: integer
      - in: query
        name: page_size
        type: integer
      - in: query
        name: sort_criteria
        type: string
      - in: query
        name: sort_order
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.PaginationDataResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: List withdraw proposals
      tags:
      - Withdrawal
    post:
      consumes:
      - application/json
      description: Prepare and create a new withdraw proposal on-chain
      parameters:
      - description: 'Withdraw Proposal details (e.g., '
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/request.CreateWithdrawProposalRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Create a new withdraw proposal
      tags:
      - Withdrawal
  /withdraw-proposals/{id}:
    get:
      consumes:
      - application/json
      description: Retrieve details of a specific withdraw proposal by its ID
      parameters:
      - description: Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.WithdrawProposalResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      summary: Get a withdraw proposal
      tags:
      - Withdrawal
  /withdraw-proposals/{id}/confirm:
    post:
      consumes:
      - application/json
      description: Finalize and confirm a specific withdraw proposal by ID
      parameters:
      - description: Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm a withdraw proposal
      tags:
      - Withdrawal
  /withdraw-proposals/{id}/main-pool-confirm:
    post:
      consumes:
      - application/json
      description: Finalize and confirm a specific withdraw proposal by ID from main
        pool
      parameters:
      - description: Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      - description: Bank Transaction Capture Image Blob ID
        in: query
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Confirm a withdraw proposal from main pool
      tags:
      - Withdrawal
  /withdraw-proposals/{id}/vote:
    post:
      consumes:
      - application/json
      description: 'Submit a vote for a specific withdraw proposal. Note: This uses
        query parameters for the vote data.'
      parameters:
      - description: Withdraw Proposal ID
        in: path
        name: id
        required: true
        type: string
      - in: query
        name: is_vote_yes
        required: true
        type: boolean
      - in: query
        name: refuse_reason
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.BuildTransactionResponse'
        "400":
          description: Invalid data. Please try again.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "401":
          description: You have no rights to access this action.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
        "500":
          description: There is something wrong in the system during the process.
            Please try again later.
          schema:
            $ref: '#/definitions/response.MessageAPIResponse'
      security:
      - BearerAuth: []
      summary: Vote on a withdraw proposal
      tags:
      - Withdrawal
schemes:
- http
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
